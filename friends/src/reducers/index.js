
import { LOGIN_START } from "../actions";

const initialState = {
  friends: [],
  error: ''
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
